server:
  port: 10030

feign:
  httpclient:
    enabled: true

spring:
  application:
    name: 'Spring boot boot app'

greeting:
  url: "http://localhost:10020"

azure:
  application-insights:
    instrumentation-key: "I-KEY"
    web:
      enabled: true
      w3c: true
    logger:
      level: TRACE